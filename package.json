{
  "name": "pashuvision-fullstack",
  "version": "1.0.0",
  "description": "PashuVision - AI Livestock Management System",
  "main": "api/index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "node api/index.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "22.x",
    "npm": ">=8.0.0"
  },
  "packageManager": "npm@8.19.4"
}
